import { useState } from 'react'; import { euler } from '../services/odeMethods'; import GraphPlot from '../components/GraphPlot'; import TableOutput from '../components/TableOutput'; function EulerMethodPage() { const [results, setResults] = useState([]); function handleSolve() { const f = (x, y) => -2 * y; const res = euler(f, 0, 1, 0.2, 5); setResults(res); } return (<div><h1>Euler's Method</h1><button onClick={handleSolve}>Solve</button>{results.length > 0 && (<><GraphPlot data={results} /><TableOutput data={results} /></>)}</div>); } export default EulerMethodPage;